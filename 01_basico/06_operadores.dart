void main() {

  //op de asignacion
  // = asigna un valor a una variable

  // b ??= 20 asigna el valor si la variable es null

  // op condicionales

  // TERNARIO c > 25 ? "xd" : "siuu";

  //  b ?? a ?? 100 y asi sucesivamente se puede usar varias

  // relaacionales 
  // todos retornal bool

  // > mayor que
  // < menor que
  // >=
  //<=
  // == compara igualdad
  // != si son diferentes

  // op de tipo

  // print(i is int) op is para verificar el tipo de dato
  // print(i is! int) op is para verificar la negacion tipo de dato
  
}